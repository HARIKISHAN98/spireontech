/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
};

// next.config.mjs
images: {
  domains: ['images.unsplash.com']
};


export default nextConfig;
